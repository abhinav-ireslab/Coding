package Testing;

public class MixStringsExercise
{
	public static void MixStrings(String s, String t) {
		//Example : s= ABC, t = DEF ==> ADBECF 
		String L ;
		String next ;
		if (s.length() == t.length()) {
			L = s + t;
				} else {
					return;
					}
					for(int i = 0; i <= s.length(); i ++ ){
			return; 
						}
	}
}